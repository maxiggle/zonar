name: zonar
description: "Submarine battle game with Midnight ZK zero-knowledge proofs."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flame: 1.37.0
  flutter_bloc: ^8.1.6
  webview_flutter: 4.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
